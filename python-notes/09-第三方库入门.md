# 09-第三方库入门

## 1. pip 包管理器

```bash
# 安装包
pip install requests

# 指定版本
pip install numpy==1.24.0

# 升级
pip install --upgrade requests

# 卸载
pip uninstall requests

# 查看已安装
pip list

# 导出依赖
pip freeze > requirements.txt

# 安装依赖
pip install -r requirements.txt
```

`requirements.txt` 示例：
```
requests>=2.28.0
numpy==1.24.0
pandas
```

## 2. requests — HTTP 请求

```python
import requests

# GET 请求
response = requests.get("https://api.github.com")
print(response.status_code)  # 200
print(response.json())       # 转为 Python 字典

# 带参数
params = {"q": "python", "page": 1}
response = requests.get("https://httpbin.org/get", params=params)

# POST 请求
data = {"username": "alice", "password": "secret"}
response = requests.post("https://httpbin.org/post", json=data)

# 添加请求头
headers = {"User-Agent": "MyApp/1.0"}
response = requests.get("https://httpbin.org/headers", headers=headers)
```

## 3. 数据处理（简要了解）

```python
# NumPy: 数值计算
import numpy as np

arr = np.array([1, 2, 3, 4, 5])
print(arr.mean())  # 平均值
print(arr * 2)     # 向量化运算

# Pandas: 表格数据处理
import pandas as pd

df = pd.DataFrame({
    "name": ["Alice", "Bob", "Charlie"],
    "age": [25, 30, 35],
    "score": [85, 90, 78]
})

print(df.head())        # 查看前几行
print(df["age"].mean()) # 列的平均值
print(df[df["score"] >= 80])  # 筛选
```

## 4. 虚拟环境与依赖管理

```bash
# 创建虚拟环境
python -m venv venv

# 激活
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 退出
 deactivate
```

## 5. 更多值得学习的库

| 领域 | 推荐库 |
|------|--------|
| Web 开发 | Flask, Django, FastAPI |
| 数据分析 | Pandas, NumPy, Matplotlib |
| 机器学习 | scikit-learn, TensorFlow, PyTorch |
| 爬虫 | requests, BeautifulSoup, Scrapy |
| 自动化 | Selenium, playwright |
| 办公自动化 | openpyxl（Excel）, python-docx |

---

## 笔记区

- 养成用虚拟环境的习惯，避免包版本冲突
- 学习新库时先看官方 Quick Start
